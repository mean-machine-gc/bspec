{
  "name": "ubispec",
  "version": "0.1.0",
  "description": "Ubiquitous specification format for describing software systems behaviour",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "gen:schema": "tsx schema/generate-json-schema.ts",
    "gen:docs": "tsx schema/generate-docs.ts",
    "gen:all": "npm run gen:schema -- lifecycle v1.0 && npm run gen:schema -- process v1.0 && npm run gen:docs -- lifecycle v1.0 > spec/lifecycle/v1.0-tables.md && npm run gen:docs -- process v1.0 > spec/process/v1.0-tables.md"
  },
  "author": "Giovanni Chiodi",
  "license": "MIT",
  "devDependencies": {
    "tsx": "^4.21.0",
    "zod": "^4.3.6",
    "zod-to-json-schema": "^3.25.1"
  }
}
